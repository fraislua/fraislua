# CLAUDE.md

このファイルは、このリポジトリ（`fraislua/fraislua`）で作業する際にClaude Codeが参照するプロジェクト固有の指示です。

## このリポジトリについて

GitHubの特殊なProfileリポジトリ。`README.md`がそのままGitHubプロフィールページに表示される。
用途は就活ポートフォリオ（ゲームプランナー / ゲームプログラマー志望）。想定読者は主に採用担当者・エンジニア。

これはUnity/C#の開発リポジトリではない。ゲーム開発向けのdefensive programmingやUniRx/UniTaskのライフサイクル管理などの指示はここでは適用しない。

## ディレクトリ構成

- `README.md` — メインの看板ページ
- `docs/thinking/` — 思考ログ記事（日々の考察を公開向けに整形したもの）
- `docs/projects/` — 個別プロジェクトの深掘り記事
- `assets/` — README等から参照する画像等
- `_source/` — 記事化前の生の思考ログ・資料。**`.gitignore`対象。絶対にコミットしない**

**新しいディレクトリやファイルカテゴリを追加した場合は、この一覧も併せて更新する。**

## 編集方針

### 個人情報の扱い
- 本名は掲載しない。名乗る場合はGitHubハンドル（fraislua）を使う
- 所属は「大学3回生」程度の一般化に留め、大学名・学部名などの特定情報は書かない
- 活動拠点（市区町村、キャンパス名等）の具体的な言及はしない
- 就活中の企業名は、選考への影響を考慮し一般化するか削除する
- 内部識別子（Google DriveのフォルダID、スケジュールの内部UUID等）は掲載しない

### 実績・記述の正確性
- `_source/`に書かれていない実績・数値・評価を推測で足さない
- 断定できない場合は「〜を目指している」程度の表現に留める、または書かない
- プロジェクトの状態（公開済み/制作中など）は、資料の文脈から推測せず、不明な場合はオーナー本人に確認する
  - 過去の失敗例: ログに「EclipseDiverで試遊評価を受けた」とあっただけで、実際には別プロジェクト（進行中の制作物）の話だったのを「EclipseDiverは制作中」と誤って書いてしまったことがある

### 文体
- 思考ログとしての生々しさ（考察の過程）は残しつつ、内輪ネタ・日付固有の言及は一般化・削除する
- 誇張表現・過度にカジュアルな文体は避ける
- Markdownの見出しレベル・リストのネストが崩れていないか自己チェックする

### リンク
- サイト内の相対リンクは常に相対パス（例: `./docs/thinking/01_...md`）で記述し、絶対URLにしない
- 外部サービス（Steam, unityroom等）へのリンクは絶対URLでよい

## Git運用

- コミットは作業の区切りごとに行ってよい
- **pushは必ずユーザーの明示的な確認を得てから行う。過去に承認されたからといって、次回以降のpushを無断で行わない（1回ごとに確認する）**
- **pushする前に、README.md末尾の「最終更新日: YYYY-MM-DD」を当日の日付に更新する**（コンテンツに変更がある場合のみ。typo修正など軽微な変更でも都度更新してよい）
- コミットメッセージは英語、末尾に `Co-Authored-By: Claude Sonnet 5 <noreply@anthropic.com>` を付与する

## 現状スナップショット（作業前に実体を確認すること）

- 技術スタック: Unity (C#), DOTween, UniTask, UniRx / Proxmox VE, Ubuntu Server, PaperMC, Tailscale / Claude Code, Gemini Spark, LM Studio
  - VContainerは実際には未使用のため技術スタックに含めない
- Gemini Sparkは自作エージェントではなく、Geminiアプリのエージェント機能（Claude Codeのcoworkに近い機能）を活用して運用しているもの。「自作」という表現は使わない
- プロジェクト:
  - EclipseDiver: 公開済み（2026年春 Unityroom Game Jam作品）。短いゲームループと「見ていても飽きない」見せ方を重視
  - Project_Orbital: 制作中（タワーディフェンス）。詳細は今後追記予定
- README「2. 設計哲学」セクションはオーナー本人がまだ言語化中。指示がない限りむやみに拡張しない

このスナップショットは作成時点の内容。編集前に必ずREADME.md等の実体を読み、齟齬があれば実体を優先しつつオーナーに確認する。
